// package Day8;

public class AccessModifier {
    public static void main(String[] args) {
       /*  Access Modifiers
        Public : any member defined as Public will be accessible in All / External Classes across
                 Packages.
        Private: any member defined as Private will be Accessible only in the Declaring Class.
        Protected : any Member defined as Protected will accessible in Derived class Only across
                     Packages.
        Default : this is the default access (no keyword required) such members are accessible
        
        across all classes in the Same Package Only.
*/

    }
}
